package Algorithms;

import edu.princeton.cs.algs4.Queue;
import DataStructures.Interfaces.Graph;

/**
 * Procedure for finding the MST, given some undirected graph G.
 */
public class PrimsAlgorithm {
    private class Node {
        private int label, weight;
    }

    /** Return the MST (still technically a graph...) corresponding to graph G. */
    public static Graph getMST(Graph G) {

        // Wow, such progress!
        Queue<Node> fringe = new Queue<>();



    }


}
