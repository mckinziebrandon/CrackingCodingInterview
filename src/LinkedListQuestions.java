import DataStructures.LinkedList;

/**
 * Questions from Chapter 2 of CTCI.
 */
public class LinkedListQuestions {

    /** 2.1 -- Remove Dups.
     * Write code to remove duplicates from an unsorted linked list.
     *
     * Brute force: O(n^2) search. Gross.
     * Better: sort the list, and then O(N) check for dups. Overall O(N log N).
     *
     * If no temp buffer allowed, then my first instinct is to do an in-place sort.
     * @param list  unsorted linked list.
     */
    public static void removeDups(LinkedList list) {

    }

}
